"""The example run files, packaged so that they reach every user.

This directory holds data, not code: each `*.toml` file here is a
complete, ready-to-run run file (design section 10). It is a package
only so that the files are installed along with the library and can be
found through it (`scattering.cli.examples`), which is what lets
`scsim rutherford` and `scsim --examples` work identically in a clone,
in a shared `physdemo` suite, and in a `pip`-installed copy
(ARCHITECTURE 9.1). The repository's top-level `runs` is a symbolic
link to this directory.

Attribution: this module is part of the scattering teaching tool.
"""
