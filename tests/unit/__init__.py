"""Unit tests: one module under test per file, no I/O, fast.

A unit test here should run in milliseconds and should fail for exactly one
reason. Name each file `test_<module>.py`, matching the module in
`src/<package>/` that it covers.
"""
